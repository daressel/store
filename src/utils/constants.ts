export const LocalStorageKeys = {
  screenSize: 'screenSize',
} as const;

export const BlockIds = {
  sidebarWithNavMenu: 'sidebar-with-nav-menu',
  buttonNavMenuSmallScreen: 'button-nav-menu-small-screen',
} as const;
